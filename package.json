{
  "name": "tower",
  "version": "0.1.0",
  "private": true,
  "description": "Co-op card autobattler - climb the tower together",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace shared",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "dev": "npm run build:shared && concurrently -n server,client -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm run build:shared && npm run build --workspace server && npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
